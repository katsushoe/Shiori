[package]
name = "shiori-engine"
version = "0.0.0"
edition = "2024"
license-file = "../../LICENSE"

[lib]
name = "shiori_engine"
crate-type = ["cdylib"]

[package.metadata.shiori]
version = "0.0.0.0"

[dependencies]
